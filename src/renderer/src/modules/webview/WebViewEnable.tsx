import { FC } from "react";

export const WebViewEnable: FC = () => {
  return <div className="w-full h-full flex justify-center items-center">
    Webview not enabled, click <button className="text-blue-500">here</button> to enable
  </div>
}
