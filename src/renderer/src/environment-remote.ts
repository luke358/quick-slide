// const { ipcRenderer } = window.electron;
// export async function userDataPath(...segments: string[]): Promise<string> {
//   return await ipcRenderer.invoke('get-user-data-path', segments.flat());
// }

// export async function userDataRecipesPath(...segments: string[]): Promise<string> {
//   return await userDataPath('recipes', ...[segments].flat());
// }
