import { FC } from "react";

export const WebViewHibernate: FC = () => {
  return <div className="w-full h-full flex justify-center items-center">
    Webview hibernating, click <button className="text-blue-500">Wake Up</button> to wake up
  </div>
}
